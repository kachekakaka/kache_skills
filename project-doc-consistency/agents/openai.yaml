interface:
  display_name: "项目文档一致性检查与方案"
  short_description: "核对活动文档事实并保存可独立实施与关闭的方案"
  default_prompt: "使用 $project-doc-consistency 检查活动文档与当前事实；先报告，得到明确授权后只保存一份可交接方案并停止。"

policy:
  allow_implicit_invocation: false
