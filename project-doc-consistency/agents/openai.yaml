interface:
  display_name: "项目文档一致性"
  short_description: "按文档职责对照证据检查事实，并最小同步可直接证明内容正文"
  default_prompt: "使用 $project-doc-consistency check 只读检查项目事实，或明确要求 sync 并修复以直接最小同步可证明内容。"

policy:
  allow_implicit_invocation: false
