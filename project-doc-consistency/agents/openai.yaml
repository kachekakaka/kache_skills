interface:
  display_name: "项目文档一致性建议审计"
  short_description: "只读核对活动文档声明与一层直接事实并报告冲突和建议"
  default_prompt: "使用 $project-doc-consistency 只读核对当前活动文档与一层直接事实，并简短报告不一致。"

policy:
  allow_implicit_invocation: false
