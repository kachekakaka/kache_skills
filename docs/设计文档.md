# 设计文档

本文件承接当前包结构、调用关系、证据边界和验证入口。用户可观察合同见[需求文档](需求文档.md)。

## 模块结构

```text
project-doc-skeleton/
├── SKILL.md
└── agents/openai.yaml

project-doc-consistency/
├── SKILL.md
└── agents/openai.yaml

project-doc-contraction/
├── SKILL.md
└── agents/openai.yaml

SoftwareTesting/doc_consistency/
├── README.md
├── install_project_doc_skills.py
├── test_markdown_links.py
├── test_project_doc_skill_responsibilities.py
└── test_project_doc_skill_installer.py

SoftwareTesting/manual/project_doc_skills/
├── README.md
└── contract-cases.md
```

每个 Skill 都是自包含包。`SKILL.md` 是唯一运行合同；`agents/openai.yaml` 只提供界面信息和显式调用策略。不存在第四个共享
Skill、共享长审计协议、随附 T-DOC 或目标项目模板。上图记录当前文件，不是禁止未来合理支持文件的永久白名单。

## 调用与退出

```text
用户显式调用对应 Skill
→ 读取适用项目规则与当前文档入口
→ 指定范围：只检查指定活动文档
  未指定范围：从项目入口识别并检查全部活动文档
  范围无法可靠形成：报告未覆盖，不全仓扫描
→ 每个问题最多读取一层直接证据
→ 输出简短问题清单、建议和未覆盖边界
→ 停止，不写文件、不形成方案、不实施
```

三个 Skill 不自动调用彼此。普通任务可以根据报告另行讨论、形成方案或实施，但不能继承审计请求的写入授权。

## 三类判断

- Skeleton 只判断入口／导航是否有效、所有者是否竞争、活动与历史是否混淆，以及本项目明确声明的结构职责是否无人承接。
- Consistency 只判断活动声明与一层直接事实是否一致；无法由当前权威裁决时保留冲突和选项。
- Contraction 只判断是否有证据支持删除、迁移、合并、摘要或简化；事实、所有者或消费者不清时保留内容。

三者都使用相同反过度治理门。新增长期资产必须有具体保护对象、真实消费者、合理失败、现有证据缺口和删除条件，否则
只能作为可选建议。

## 安装设计

`install_project_doc_skills.py` 只在明确安装请求下使用。三个受支持名称都直接映射到自身唯一安装根；安装器只接受干净、
已提交的源仓库，精确镜像目标目录并在其中生成 `SOURCE-PROVENANCE.json`。`--verify-only` 可以检查来源提交、清单和文件
SHA-256，但该机制只证明安装来源与漂移，不是 Skill 运行前置，也不证明报告正确。本仓库只承诺当前 Windows 环境，其他
系统不主动阻断但不提供兼容性承诺。

## 验证设计

普通验证只包含三个直接能力：

1. `test_markdown_links.py` 以小型自测试证明链接判断，再检查本仓库 `archive/` 历史根以外 Markdown 的本地相对链接和
   标题锚点；外链不联网验证，也不要求固定文件、目录、Registry 或生命周期。
2. `test_project_doc_skill_responsibilities.py` 检查三个包名称、显式调用元数据，以及运行入口的本地依赖没有逃出各自包；不匹配
   生成文案，也不禁止整个支持文件目录类别。
3. `test_project_doc_skill_installer.py` 从三个受支持名称分别验证单包精确镜像，以及来源清单的 `verified`、`mismatch` 和
   `missing` 结果；不锁定安装器的内部映射容器。

[可选行为样例](../SoftwareTesting/manual/project_doc_skills/README.md)只在用户显式授权时运行；未运行时，普通验证只能证明
源码合同和包装边界，不能声称真实模型行为已经通过前向试用。

## 架构决策记录

- 现行：[ADR-0007：按副作用而不是工具名称划分授权](adr/0007-authorize-by-side-effect.md)
- 现行：[ADR-0015：项目文档 Skill 只做建议型审计](adr/0015-document-skills-are-advisory-auditors.md)
- 现行：[ADR-0016：Skeleton 不再交付通用文档门禁](adr/0016-stop-shipping-the-generic-doc-gate.md)
- 现行：[ADR-0017：默认完整检查活动文档并交付简洁报告](adr/0017-full-active-docs-compact-report.md)
- 现行：[ADR-0018：建议型审计判断证据问题而不是治理合规](adr/0018-evidence-not-governance-compliance.md)
- 现行：[ADR-0019：保留 Skill 名称并只验证可观察边界](adr/0019-compatible-names-and-behavioral-checks.md)
- 历史：[ADR-0001～ADR-0006、ADR-0008～ADR-0014](../archive/docs/adr/) 已被替代并退出活动入口；仍有效的职责、授权和
  生命周期合同由上述现行 ADR、`AGENTS.md`、本设计及需求文档承接。
