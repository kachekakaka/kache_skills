# SKELETON-ACCEPTANCE-DELTAS-20260804：骨架 Skill 独立验收差额收敛方案

- 原待办：`SKELETON-ACCEPTANCE-DELTAS-20260804`（已按“被替代”退出活动列表）
- 状态：已被替代
- 方案类型：独立验收后的最小规则收敛
- 授权说明：用户已确认四项规则决定、授权保存本方案，并在后续回合明确授权实施；实施授权只覆盖第 4.1 节七个路径及必要的待办和方案状态写入，不包含命令、Git、验证、安装、关闭或清理。2026-08-04 用户另行确认由 `SKELETON-PLAN-ONLY-20260804` 替代本方案，并授权本方案退出活动生命周期

## 1. 初始检查证据（历史快照）与验收差额

- 独立只读验收结论为“建议修改后通过”，没有阻断问题；核心三阶段、授权包停点、事实后写和可中断关闭机制已经成立。
- 2026-08-04 的隔离前向试用中，精确空目录第一次删除失败后又在同一授权内尝试一次，随后才停止。该行为没有扩大目标或实际删除，但与用户已经确认的“失败即停止”语义不完全一致。
- 同一试用的归档方案把施工前状态写成“当前结构证据”；实际结果仍准确记录为 `关闭中` 和最终 T-DOC `not_run`，没有虚构完成，但历史证据、完成条件和当前结果的标签不够清楚。
- [`project-doc-skeleton/SKILL.md`](../../project-doc-skeleton/SKILL.md) 同时保留核心阶段规则和较多授权、验证、关闭细节；[`skeleton-rules.md`](../../project-doc-skeleton/references/skeleton-rules.md) 与 [`testing-baseline.md`](../../project-doc-skeleton/references/testing-baseline.md) 又重复承接部分相同细节，存在上下文负担和后续漂移风险。
- `SKILL.md`、根 [`README.md`](../../README.md)、[`docs/需求文档.md`](../../docs/需求文档.md) 和前向契约使用“新一轮首次调用”等表述，但没有统一说明判断对象是新问题与对应活动方案，而不是对话或 Skill 调用次数。
- 隔离试用现场保留在仓库外作为原始证据，本方案不修改或清理该现场。保存前在当前仓库可读文件中未发现候选 ID 的既有复用；本轮没有运行 Git，不能据此声明 Git 历史已核对。

## 2. 已确认决定

| 差额 | 可选处理 | 推荐理由 | 用户决定 |
| --- | --- | --- | --- |
| D-01：失败后的同授权重试 | A. 授权包写明“失败即停止”时，第一次实际失败后立即停止，只有用户再次明确授权才能重试；B. 默认允许同目标自动重试一次 | A 使授权次数和失败停点可观察，避免把一次授权扩张为未披露的多次尝试 | 已确认 A |
| D-02：历史证据与当前结果 | A. 施工前内容标为“初始检查证据（历史快照）”，施工后事实写入“实施结果”或“关闭结果”，完成条件只作为判定标准；B. 继续混用“当前结构证据”和完成条件 | A 同时保留审计历史与当前事实，不让条件冒充结果 | 已确认 A |
| D-03：三份规则的唯一职责 | A. `SKILL.md` 只保留工作阶段、必须停下的节点和核心边界；`skeleton-rules.md` 详细承接授权与生命周期关闭；`testing-baseline.md` 详细承接验证项目与证据记录；B. 三处继续复制完整规则 | A 符合渐进披露并降低重复与漂移 | 已确认 A |
| D-04：“首次调用”的定义 | A. 仅指开始处理一个新骨架问题且尚不存在对应活动方案；同一问题后续按方案状态继续；B. 按对话或 Skill 调用次数判断 | A 避免后续保存、施工、验证或关闭回合错误重走初始流程 | 已确认 A |

## 3. 目标与非目标

### 3.1 目标

1. 固化 D-01 的失败停点和再次授权要求，不引入隐式重试。
2. 固化 D-02 的历史快照、实际结果和完成条件三类语义。
3. 按 D-03 收缩三份核心规则的重复内容，建立清楚、可直接引用的唯一详细真源。
4. 按 D-04 统一 skeleton 的首次调用定义，并同步必要的当前能力摘要和前向契约。

### 3.2 非目标

- 不改变检查、保存、施工三个阶段，不放宽任何文件、命令、Git、安装、联网、产品进程、真实数据、删除或关闭授权。
- 不改变标准骨架与等价既有治理路线、目录要求、Registry schema、T-DOC 规则或门禁实现。
- 不修改 `project-doc-consistency` 的职责、规则或前向契约；共享前向文档只调整 skeleton 专属段落和断言。
- 不借本方案补做成功关闭、Git/临时索引、等价路线或全项目测试与安全认证。
- 不修改、修复或清理仓库外隔离试用现场，也不改写已经归档的历史方案。

## 4. 目标路径与动作边界

### 4.1 确定修改路径

| 路径 | 计划动作 | 精确边界 |
| --- | --- | --- |
| `project-doc-skeleton/SKILL.md` | 收缩并澄清 | 保留阶段、硬停点和核心边界；明确首次调用定义；详细授权、验证和关闭规则改为直接引用对应参考，不复制全文 |
| `project-doc-skeleton/references/skeleton-rules.md` | 收敛职责 | 作为授权包、失败停点、再次授权和生命周期关闭顺序的详细真源；直接引用测试基线中的验证项目与证据规则 |
| `project-doc-skeleton/references/testing-baseline.md` | 收敛职责 | 作为验证项目、执行事实、结果状态和证据记录的详细真源；引用骨架规则中的授权与关闭语义，不再重复完整授权流程 |
| `README.md` | 最小同步 | 只澄清 skeleton 首次调用的判断依据并保持高层能力摘要，不复制详细规则 |
| `docs/需求文档.md` | 最小同步 | 只更新 skeleton 当前支持边界中与四项决定直接相关的摘要，不扩写施工细节 |
| `SoftwareTesting/manual/project_doc_skills/README.md` | 最小更新 | 让 skeleton 修改型样本和硬断言观察四项决定；只写可观察结果并引用规则真源 |
| `SoftwareTesting/manual/project_doc_skills/contract-cases.md` | 最小更新 | 在现有最小夹具中加入首次调用定义、失败后不自动重试和历史/结果区分，不增加项目或模式矩阵 |

### 4.2 生命周期路径

- 保存阶段只新增本方案，并由 [`docs/已知问题与待做需求.md`](../../docs/已知问题与待做需求.md) 的同 ID 待办链接；两者保持 `待确认`。
- 施工开始后按实际状态更新该待办和本方案，不用保存授权推定施工授权。
- 只有验收通过且用户另行授权关闭后，才退出活动待办，将本方案精确归档到 `archive/docs/2026-08-04-SKELETON-ACCEPTANCE-DELTAS-20260804-骨架Skill独立验收差额收敛方案.md`，更新 `archive/docs/README.md`，处理获批的精确空条件目录并运行最终状态门禁。

### 4.3 明确不修改

- `project-doc-skeleton/agents/openai.yaml`：当前显示名、显式触发和默认提示仍与高层能力一致，只做静态匹配复核。
- `project-doc-skeleton/assets/SoftwareTesting/doc_consistency/` 与 `SoftwareTesting/doc_consistency/`：本方案不改变 T-DOC 源码或规则。
- `docs/README.md`、`docs/设计文档.md`、`AGENTS.md`、`project-doc-consistency/` 和既有归档方案：当前职责不因四项决定变化。
- 仓库外隔离目标及其归档方案：继续保留原始试用结果，不回写修复。

## 5. 拟定规则结果

1. 一个新骨架问题且尚无对应活动方案时进入首次只读检查；同一问题已有活动方案时，根据 `待确认`、`实施中` 或归档过渡事实继续，不按消息数量重置阶段。
2. 授权包明确约定“失败即停止”时，任何实际失败都结束当前执行；相同目标、相同动作也不得在原授权下重试，后续重试需要用户再次明确授权。
3. 方案保存时的结构证据属于当时快照；发生施工后，快照保留并明确标为历史，实际变化和关闭事实分别进入结果段。完成条件不能自动转换为 `passed`、`已清理` 或 `已完成`。
4. `SKILL.md` 负责告诉执行者当前阶段、必须停在哪里以及何时完整读取参考；授权和生命周期细节只在 `skeleton-rules.md` 完整定义，验证项目和证据语义只在 `testing-baseline.md` 完整定义。
5. README、需求摘要和前向契约可以保留必要的可观察行为，但不复制授权包字段、验证账本或关闭顺序全文。

## 6. 完成条件

- 四项用户决定在全部确定修改路径中语义一致，没有相反表述或第二套详细规则。
- 第一次实际失败后任务停止；任何重试都能明确追溯到后续用户授权。
- 活动与归档方案能区分初始检查证据、实施/关闭结果和完成条件，未执行项保持 `not_run`。
- skeleton 的首次调用只按新问题和对应活动方案判断；同一问题的后续阶段不会因新用户回合或再次显式调用而重置。
- 三份核心规则形成清晰导航且没有完整规则重复；README、需求文档与前向契约只保留必要摘要或断言。
- `agents/openai.yaml`、T-DOC 资产、标准/等价路线和 consistency 职责没有意外变化。

## 7. 验证要求

- 测试层级：普通验证。
- 验证影响域：project-doc-skeleton 的阶段识别、失败停点、授权与关闭、验证证据语义、当前能力摘要、前向试用契约，以及本仓库待办与活动方案机械关系。
- 具体验证项：Skill 目录和 frontmatter 结构校验；T-DOC 规则夹具；当前仓库目标 T-DOC；两份标准 T-DOC 资产逐字节比较；对七个确定修改路径、`agents/openai.yaml` 和生命周期文件做静态一致性复核；关闭后的最终 T-DOC。候选施工和普通验证完成后再做一次简短独立只读验收。

`T-PROJECT-DOC-FORWARD` 保持 `explicit`。本次最小修订不自动要求重新执行完整前向试用；只有用户明确加入时才运行，未运行时记录 `not_run`。成功关闭、Git/临时索引和等价路线仍作为未覆盖范围，不以普通验证或静态复核冒充已证明。

## 8. 实施前置条件与授权边界

1. 施工前重新读取适用 `AGENTS.md`、本方案和七个确定修改路径，确认候选没有实质漂移。
2. 用户需在保存后再次明确授权实施；施工授权仅覆盖第 4.1 节确定路径和实施阶段必要的待办/方案状态写入。
3. 命令、只读 Git 枚举、临时文件、验证、安装、联网、产品进程、删除和生命周期关闭仍按实际目标另行明确；可以形成一次自包含申请，但不得相互推定。
4. 如果实施需要第 4.1 节之外的文件、改变已确认决定或修复隔离试用现场，停止并重新冻结方案范围。

## 9. 原计划的收尾与联合复核

- 本方案主责与完成证明：只证明四项独立验收差额已经按用户决定收敛，三份规则职责唯一且必要消费者一致；不证明未覆盖的成功关闭、Git、等价路线、全项目测试充分性或安全认证。
- 关闭时的状态消费者：`README.md`、`docs/需求文档.md`、`docs/已知问题与待做需求.md`、本活动方案和 `archive/docs/README.md`；其他长期文档只有出现直接引用冲突时才报告差额，不自动纳入修改。
- 关联方案、共享文件与对方职责状态：保存时识别的关联历史方案为已关闭的 `SKELETON-AUTH-FLOW-20260803`；两份前向文档同时包含 consistency 契约，但本方案不改变其段落。保存时没有识别关联活动方案，因此原计划判断跨 Skill 联合复核不适用。
- 联合只读复核触发条件：若施工中发现 consistency 专属断言或其他活动方案必须同步，停止并新增明确关联；否则只进行本方案的独立只读验收，不自动调用另一个 Skill 或继承历史方案结论。
- 验收停点：文件施工与获批的普通验证完成后停止，等待独立只读验收和用户确认；不在施工或验证回合自动关闭。
- 关闭边界与未运行验证：关闭只处理第 4.2 节精确生命周期路径、已披露的空目录和最终 T-DOC；所有未执行验证保持 `not_run`，失败或中断按实际状态报告，不预写未来结果。

## 10. 实施结果

- 四项规则决定已由用户逐项确认。
- 本方案及活动待办已转为 `实施中`；七个确定修改路径的文件施工已经完成。
- 静态施工复核为 `passed`：三份核心规则已按授权与生命周期、验证与证据两类详细真源分工；必要消费者和 skeleton
  前向断言与四项决定一致，`agents/openai.yaml` 仍匹配高层能力，没有发现需要扩大方案范围的差额。
- 普通验证、显式前向试用、独立验收、Git 枚举和关闭均为 `not_run`。
- 本轮没有运行命令、Git、安装、联网、测试或产品进程；没有修改 T-DOC 资产、`agents/openai.yaml`、consistency
  专属内容、既有归档方案或隔离试用现场。

## 11. 被替代退出结果

- 用户确认由 [`SKELETON-PLAN-ONLY-20260804`](2026-08-04-SKELETON-PLAN-ONLY-20260804-骨架Skill仅检查与落方案职责收缩方案.md) 承接后续 skeleton 职责收缩，并单独授权本方案按“被替代”退出活动生命周期。
- 本待办已从活动列表移除，本方案已移入归档并在归档索引登记；状态为 `已被替代`，不是 `已完成`。
- 已实施的七个文件候选没有回退；它们与后续目标之间的差额、验证和最终结论由新方案重新冻结，不继承本方案的授权或验收结论。
- 普通验证、显式前向试用、独立验收、Git 枚举和最终 T-DOC 均为 `not_run`；被替代退出不把这些项目改写为通过。
- `docs/方案/` 仍由新活动方案使用，不是空目录，本次没有目录删除或清理。
