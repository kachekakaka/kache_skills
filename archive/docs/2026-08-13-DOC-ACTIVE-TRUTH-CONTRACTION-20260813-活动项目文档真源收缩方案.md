# 活动项目文档真源收缩方案

- 待办 ID：`DOC-ACTIVE-TRUTH-CONTRACTION-20260813`
- 最终状态：已完成并归档，活动待办与方案入口已经退出
- 保存日期：2026-08-13
- 交付路线：完整只读审计后形成方案；本次保存不开始实施
- 方案交付前自查：用户选择两轮，实际完成 `2/2` 轮
- 实施完成后反哺报告：不生成
- 实施授权：用户后续明确授权按本方案实施，确定修改、普通验证和生命周期关闭已完成
- 动态验证：T-DOC 在实施中候选和关闭后候选均为 `passed`；`T-PROJECT-DOC-FORWARD`、全量测试和正式认证为 `not_run`
- 容器所有权：`docs/方案/` 保存前不存在，由本次最终保存阶段创建；后续普通实施任务归档本方案并确认目录为空后，可以按项目规则精确删除该空目录

## 1. 目标与当前结论

在不改变两个项目文档 Skill 用户可观察行为的前提下，收缩活动项目文档的重复真源：让需求只拥有用户可验收合同，
设计只拥有实现结构与路由，术语表只负责消歧，项目文档入口只负责导航和生命周期，测试协议只负责验证治理，前向
试用入口只负责 suite 协议；完整运行规则继续由两个 `SKILL.md` 及其按条件加载的 references 实现，测试期望继续由
现有契约夹具消费。

本次冻结的 24 份活动文档合计约 1,487 行；其中 `CONTEXT.md`、`docs/README.md`、需求、设计、测试协议和前向试用
README 合计约 1,077 行。行数只是当前事实，不作为完成门槛。完成证明必须来自本方案逐项列明的重复正文、实现镜像、
程序性说明和错位职责确实退出活动真源，并且必要合同、入口和历史证据仍有唯一所有者。

## 2. 活动真源、模式与冻结范围

### 2.1 所有者

| 内容类别 | 当前唯一所有者 | 其他文档职责 |
| --- | --- | --- |
| 用户可观察、可验收行为与支持边界 | `docs/需求文档.md` | 设计、测试和入口只保留必要摘要或链接 |
| 当前实现结构、入口、引用路由和验证拓扑 | `docs/设计文档.md` | 需求不复制实现算法 |
| 两个 Skill 的完整运行规则 | 各自 `SKILL.md` 及按条件加载的 references | 活动项目文档不逐条镜像 |
| 项目协作与动作授权 | `AGENTS.md` | 测试协议只补验证层级特有规则 |
| 活动文档导航、待办／方案关系与收尾模板 | `docs/README.md` | Skill 不重新定义目标项目生命周期 |
| 项目术语 | `CONTEXT.md` | 只定义和消歧，不承接完整程序 |
| 架构决定及替代关系 | `docs/adr/*.md`，由设计文档索引 | 需求和设计不重复保存完整理由 |
| 验证层级与结果语义 | `SoftwareTesting/PROTOCOL.md` | Suite README 只记录专项入口 |
| 测试安全 | `SoftwareTesting/SAFETY.md` | 其他测试文档引用公共规则 |
| 前向试用身份、五样本选择、隔离和结果合同 | `SoftwareTesting/manual/project_doc_skills/README.md` | `contract-cases.md` 保存详细夹具和评估者预期 |
| 历史 Markdown 登记与当前承接 | `archive/docs/README.md` | 归档正文只提供历史语境 |

### 2.2 模式与精确成员

审计模式为 `full`。活动成员从项目入口、职责声明和直接导航冻结为以下 24 条精确路径：

1. `AGENTS.md`
2. `CONTEXT.md`
3. `README.md`
4. `docs/README.md`
5. `docs/需求文档.md`
6. `docs/设计文档.md`
7. `docs/已知问题与待做需求.md`
8. `docs/软件测试.md`
9. `SoftwareTesting/README.md`
10. `SoftwareTesting/PROTOCOL.md`
11. `SoftwareTesting/SAFETY.md`
12. `SoftwareTesting/doc_consistency/README.md`
13. `SoftwareTesting/manual/project_doc_skills/README.md`
14. `docs/adr/0001-separate-document-structure-and-content-audit.md`
15. `docs/adr/0002-prove-equivalent-governance-from-current-evidence.md`
16. `docs/adr/0003-separate-structure-governance-from-content-consistency.md`
17. `docs/adr/0004-separate-review-plan-and-construction.md`
18. `docs/adr/0005-skeleton-ends-after-plan-handoff.md`
19. `docs/adr/0006-consistency-ends-after-plan-handoff.md`
20. `docs/adr/0007-authorize-by-side-effect.md`
21. `docs/adr/0008-consistency-plan-coordinates-cross-responsibility-slices.md`
22. `docs/adr/0009-review-plan-candidate-before-saving.md`
23. `docs/adr/0010-route-report-plan-and-separate-action-authorization.md`
24. `archive/docs/README.md`

### 2.3 直接实现、测试与生命周期证据

- 产品实现：`project-doc-skeleton/SKILL.md`、`project-doc-skeleton/references/*.md`、
  `project-doc-consistency/SKILL.md`、`project-doc-consistency/references/*.md` 和两个 `agents/openai.yaml`。
- 测试实现：`SoftwareTesting/manual/project_doc_skills/contract-cases.md`、T-DOC 的 README 与两份 Python 实现。
- 五样本当前身份补证：最近一次完成记录明确列出 `CASE-CONSISTENCY-FULL`、Registry 全局、
  `CASE-CONSISTENCY-PLAN-HANDOFF`、成熟非标准 Skeleton 矩阵和 `CASE-PROJECT-SKILL-ISOLATION`。
- 生命周期：活动待办当前没有方案链接，`docs/方案/` 保存前不存在；归档索引登记的 36 份归档正文与当前文件集合
  数量相等。

### 2.4 排除项

- 归档正文只作必要历史语境，不进入逐文档双遍；`archive/docs/README.md` 因被活动入口明确指定为当前登记索引而纳入。
- 两个 Skill、references、测试夹具和 Python 脚本是实现或直接证据，不是活动项目文档，不对其正文执行内容收缩。
- 不扫描其他源码、全部测试、全部 CI／安全实现、其他仓库或 F2。
- 本轮不运行测试、构建、产品、门禁或其他动态验证。

## 3. 逐成员双遍记录与完整性门

| 活动路径 | 事实／契约遍 | 职责／删除优先遍 |
| --- | --- | --- |
| `AGENTS.md` | 当前语言、入口和副作用授权规则有效 | 项目协作规则唯一所有者，内容紧凑，无收缩项 |
| `CONTEXT.md` | 术语与 Skill／ADR 基本一致 | 多个术语扩写为完整程序和授权说明，形成 F-03 |
| `README.md` | 两个交付目录、无构建步骤和入口均与当前文件一致 | 根入口保持短摘要和路由，无收缩项 |
| `docs/README.md` | 核心入口、状态关系和收尾规则有效 | 复制 Skill 决策卡、候选评审和审计算法，形成 F-04 |
| `docs/需求文档.md` | 所述能力与当前 Skill 实现整体一致 | 逐条镜像运行实现、验证细节和授权程序，形成 F-01 |
| `docs/设计文档.md` | 模块树、引用文件和高层调用关系与当前实现一致 | 大段复制需求合同、Skill 算法及 references，形成 F-02；ADR 索引未显示替代状态，承接 F-08 |
| `docs/已知问题与待做需求.md` | 三项待确认验证残余与当前无完成证据相符；动态事实本轮未重验 | 唯一待办所有者，条目简洁；本次保存只增加必要待办与方案链接 |
| `docs/软件测试.md` | `T-DOC` 和 `T-PROJECT-DOC-FORWARD` 均有实际入口 | Registry 只保存稳定身份、类别、入口和职责，无收缩项 |
| `SoftwareTesting/README.md` | Protocol、Safety、Registry 和两个 suite 链接有效 | 测试总入口使用短摘要，无收缩项 |
| `SoftwareTesting/PROTOCOL.md` | 验证层级、类别和结果语义与当前规则一致 | 通用动作授权整段复制 `AGENTS.md`，形成 F-07 |
| `SoftwareTesting/SAFETY.md` | 当前测试根、数据、进程、联网和清理规则有明确合同 | 测试安全唯一所有者，内容均为专项合同，无收缩项 |
| `SoftwareTesting/doc_consistency/README.md` | 命令、输入、断言和结果语义与当前 Python 实现静态一致 | T-DOC suite 的稳定职责与专项调用，无收缩项 |
| `SoftwareTesting/manual/project_doc_skills/README.md` | `explicit`、五个代表性样本和隔离要求与 Registry／夹具整体一致 | 重复 Skill、references 和夹具的大段程序与硬断言，形成 F-05；五样本未绑定精确 case，形成 F-06 |
| `ADR-0001` | 当时的双 Skill 拆分事实有效，但同步／测试边界已被后续决定改写 | ADR 历史正文应保留；缺少当前替代摘要，承接 F-08 |
| `ADR-0002` | 等价治理、不建 profile 的核心仍有现行部分；六账本、apply／sync 等已被替代 | 保留历史原文，补部分替代状态和后继，承接 F-08 |
| `ADR-0003` | 结构／内容分责仍有效；直接修文档和测试交接范围已被 ADR-0006／0008 改写 | 保留历史原文，补状态和后继，承接 F-08 |
| `ADR-0004` | 检查、保存、施工分离核心仍有效；每次询问保存和跨职责限制已被后续决定改写 | 保留历史原文，补状态和后继，承接 F-08 |
| `ADR-0005` | Skeleton 保存后退出仍有效；候选评审和请求分流由 ADR-0009／0010细化 | 保留正文，补部分替代状态，承接 F-08 |
| `ADR-0006` | Consistency 保存后退出仍有效；综合方案范围和请求分流由后续决定改写 | 保留正文，补部分替代状态，承接 F-08 |
| `ADR-0007` | 按实际副作用授权仍是当前决定 | 独有架构理由和后果，无收缩项 |
| `ADR-0008` | 一份综合方案协调就绪跨职责切片仍是当前决定 | 独有架构理由和后果，无收缩项 |
| `ADR-0009` | 候选先评审、最终一次保存仍有效；每次检查进入方案路线已被 ADR-0010替代 | 保留正文，补部分替代状态，承接 F-08 |
| `ADR-0010` | 报告／方案分流、联合首问和动作授权分离仍是当前决定 | 独有最新决策，无收缩项 |
| `archive/docs/README.md` | 36 条归档登记对应 36 份归档正文，索引承担当前承接映射 | 历史登记唯一所有者；逐项条目是必要索引，无收缩项 |

完整性门结果：精确活动成员集合与矩阵行集合均为 24，每个成员恰有一行，每行两遍均有成员特定结论、直接证据或
残余。冻结发现前、两轮候选评审和最终保存问题前均已完成 `24/24` 对账。

## 4. 覆盖／残余矩阵

| 维度 | 适用范围 | 状态 | 直接证据与残余 |
| --- | --- | --- | --- |
| 活动所有者与入口 | 24 份活动文档 | 已覆盖 | 当前入口可以形成完整集合；归档索引已从历史正文中分离 |
| 逐成员事实／契约遍 | 24 行 | 已覆盖 | 产品事实整体一致；F-06、F-08 是当前表达与状态歧义 |
| 逐成员职责／删除优先遍 | 24 行 | 已覆盖 | F-01～F-05、F-07 为确定收缩项；其余成员有具体保留依据 |
| 产品实现映射 | 两个 Skill、references、metadata | 已覆盖 | 静态读取足以确认活动文档存在实现镜像；不审计或修改产品正文 |
| ADR 替代链 | ADR-0001～0010 | 已覆盖 | 后继 ADR 可唯一裁决 F-08，不需要新的架构语义选择 |
| 测试设计声明绑定 | Registry、前向 README、`contract-cases.md`、当前 Skill | 已覆盖 | 只核对 suite 职责、五样本选择和断言所有权；形成 F-05、F-06 |
| 结构机械断言漂移 | 当前确定收缩路径的一跳消费者 | 不适用 | 搜索未发现外部锚点绑定拟删除章节；保留 `设计文档.md#架构决策记录` |
| 生命周期承接 | 待办、方案容器、归档索引 | 已覆盖 | 新方案可处于待确认；保存不授权实施；关闭路径明确 |
| 动态通过性 | T-DOC、前向矩阵及其他运行验证 | 无法验证 | 审计阶段按 Skill 要求全部 `not_run` |
| 全量测试、正式认证、CI／安全全局完整性 | 全项目 | 未覆盖 | 不在本次结论或保存授权中，不能据此宣称完成 |

## 5. 冻结发现及完整归宿

| ID | 声明、位置与直接证据 | 结论与影响 | 候选归宿 |
| --- | --- | --- | --- |
| F-01 | `docs/需求文档.md` 逐条复述两个 Skill 的模式、决策卡、矩阵、验证和授权；相同完整规则已在两个 `SKILL.md` 及 references 实现 | 内容膨胀、实现镜像；需求变成第二份运行规范 | 确定：重写为简洁的用户可验收合同，保留可观察能力和边界，删除实现机制副本 |
| F-02 | `docs/设计文档.md` 从当前阶段模型延伸到两套逐步算法、测试审计和验证规划；直接实现已有同名主流程与 references | 内容膨胀、竞争真源；实现变化要求多处同步 | 确定：保留模块树、reference 路由、高层流程、验证拓扑和 ADR 索引，删除逐步算法与需求合同副本 |
| F-03 | `CONTEXT.md` 的多项术语把定义扩展成完整判断、询问、授权和实施规程 | 职责不清；术语文件成为隐性程序规范 | 确定：保留能区分概念的一至数句定义和必要 Avoid，删除程序性义务并链接唯一所有者 |
| F-04 | `docs/README.md` 生命周期段完整复制报告／方案分流、联合首问、决策卡格式、候选集合对账和验证规划 | 位置不当；项目生命周期入口与产品实现竞争 | 确定：只保留导航、状态与方案数量关系、保存／实施分离、关闭规则和收尾字段，其他规则引用需求、AGENTS 或 Skill |
| F-05 | 前向试用 README 的样本说明、四段运行时合同和 40 余条硬断言重复 Skill 与 `contract-cases.md` | 内容膨胀；测试入口成为产品合同副本 | 确定：保留 suite 身份、执行条件、隔离、五样本、评估过程、结果和清理；详细期望链接现有夹具 |
| F-06 | README 只用自然语言列五个代表性样本，而夹具文件包含更多补充 CASE，未给出标准矩阵到精确 ID 的映射 | 事实／契约表达不完整；新评估者可能选择不同五项 | 确定：绑定五个标准 case ID；其他 case 明确为补充回归，不计入默认五样本矩阵 |
| F-07 | `SoftwareTesting/PROTOCOL.md` 第 9～15 行复制本地读取、联网、Git、安装、删除等项目通用授权 | 重复真源；授权规则变化需双处维护 | 确定：引用 `AGENTS.md`，仅保留验证层级、类别、结果及升级需要明确选择的测试特有规则 |
| F-08 | 设计入口把十份 ADR 并列展示；ADR-0001～0006、0009 的正文包含后来被替代的当前式表述，但本文件未显示后继状态 | 事实状态含糊；读者可能采用旧 sync、范围或评审顺序 | 确定：不改历史正文，只补状态／后继摘要；设计索引标注现行与部分替代关系，文档入口改称带状态的决策记录 |

集合对账：冻结发现、覆盖／残余矩阵承接和候选处置的标识集合均为 `F-01`～`F-08`，数量均为 8；无缺失、重复或
无归宿项。

## 6. 确定实施切片

### 6.1 精确修改路径

1. `CONTEXT.md`
2. `docs/README.md`
3. `docs/需求文档.md`
4. `docs/设计文档.md`
5. `SoftwareTesting/PROTOCOL.md`
6. `SoftwareTesting/manual/project_doc_skills/README.md`
7. `docs/adr/0001-separate-document-structure-and-content-audit.md`
8. `docs/adr/0002-prove-equivalent-governance-from-current-evidence.md`
9. `docs/adr/0003-separate-structure-governance-from-content-consistency.md`
10. `docs/adr/0004-separate-review-plan-and-construction.md`
11. `docs/adr/0005-skeleton-ends-after-plan-handoff.md`
12. `docs/adr/0006-consistency-ends-after-plan-handoff.md`
13. `docs/adr/0009-review-plan-candidate-before-saving.md`

### 6.2 按所有者施工

- 需求切片：用少量验收条目承接两个 Skill 的用途、显式调用、报告／方案路线、只读检查、最终保存停点、职责边界和
  普通任务接手；删除路径枚举、判断算法、测试实现细节和逐条授权副本。不得借收缩改变任何用户可观察语义。
- 设计切片：保留真实模块树；用 reference 路由表说明哪类规则进入哪份实现；每个 Skill 只保留一条高层流程；保留两层
  验证拓扑和 `架构决策记录` 标题。删除已经由需求、Skill 或 reference 唯一拥有的详细程序。
- 术语切片：每个保留术语只回答概念是什么、与相邻概念如何区分；执行条件、问题格式、授权和验证恢复规则改为链接。
- 项目入口切片：保留核心／条件文档导航、精确待办状态与方案数量关系、保存不授权实施、关闭动作和九项收尾字段；
  删除两个 Skill 的内部候选、决策卡和发现集合算法。
- 测试协议切片：保留验证层级、影响域、类别、结果、证据失效和交接；通用动作授权改为指向 `AGENTS.md` 的短引用。
- 前向 suite 切片：保留 Registry 元数据、执行前提、五样本表、隔离／盲评、评估者和普通实施任务输入、结果与清理；
  删除逐 Skill walkthrough 和重复硬断言。五样本精确映射为：
  1. `CASE-CONSISTENCY-FULL`；
  2. `CASE-CONSISTENCY-REGISTRY-GLOBAL`；
  3. `CASE-CONSISTENCY-PLAN-HANDOFF`，包含全新普通实施任务的独立接手；
  4. `CASE-SKELETON-STRUCTURE`；
  5. `CASE-PROJECT-SKILL-ISOLATION`。
  其余 `contract-cases.md` 场景标为补充回归夹具，不进入默认五样本计数。
- ADR 切片：在 ADR-0001～0006、0009 标出部分被替代及精确后继；原始决定、理由和后果保持不改。设计索引对所有
  ADR 显示现行或部分替代状态，避免读者把历史正文当最新程序。

### 6.3 一层修改链与直接消费者

| 修改根 | 一跳消费者／约束 | 实施要求 |
| --- | --- | --- |
| `docs/需求文档.md` | 根／文档入口、设计摘要、两个 Skill 和前向测试作为消费者 | 保留现有路径；合同语义不变，不反向修改 Skill |
| `docs/设计文档.md` | `docs/README.md` 的 `#架构决策记录` 锚点和 ADR 链接 | 标题与所有 ADR 路径保持可达 |
| `CONTEXT.md` | `AGENTS.md` 项目术语入口、若干归档索引当前承接链接 | 保留文件与顶层入口，不要求历史正文改写 |
| `docs/README.md` | `AGENTS.md`、根 README、T-DOC 的必要导航 | 保留四份核心文档、归档和测试入口关系 |
| `SoftwareTesting/PROTOCOL.md` | `SoftwareTesting/README.md`、Safety 和 suite 公共引用 | 保留验证层级和结果术语锚点 |
| 前向试用 README | Registry、根／测试入口、归档当前承接、`contract-cases.md` | 保留顶层路径和 suite 身份；只收缩正文 |
| ADR-0001～0006、0009 | 设计 ADR 索引和相互后继链接 | 不移动、不改标题或历史决定，只补状态摘要 |

静态搜索未发现其他活动文档绑定拟删除章节锚点；不从上述消费者继续递归。实施修改若新产生或改变一跳链接，纳入最终
范围与漂移复核。

## 7. 可能路径、明确不修改与再冻结条件

当前没有待决语义和可能修改路径。以下内容明确不修改：

- 两个 `project-doc-*/SKILL.md`、全部 references、metadata 和 T-DOC 标准资产；
- `SoftwareTesting/manual/project_doc_skills/contract-cases.md` 与两份 T-DOC Python 实现；
- `AGENTS.md`、根 `README.md`、`docs/软件测试.md`、`SoftwareTesting/README.md`、`SoftwareTesting/SAFETY.md`、
  `SoftwareTesting/doc_consistency/README.md`、ADR-0007／0008／0010；
- 归档正文、其他源码／测试／CI／安全文件、其他仓库和 F2。

实施前重新冻结 13 个确定修改路径和本方案列明的一跳消费者。出现以下任一情况时停止并申请差额：活动所有者或路径
发生变化；需要改变用户可观察行为；五样本标准选择与现有证据不再一致；出现确定路径外必须修改的直接消费者；验证
层级或副作用升级；发生新的语义决定。

## 8. 验证规划与结果边界

- 测试层级：普通验证。
- 验证影响域：13 份确定文档、现有活动导航／锚点、ADR 后继链、五样本静态选择映射和生命周期最终状态。
- 具体验证项：实施后静态核对唯一所有者与删除清单；核对五个标准 case ID 均存在且补充夹具不进入默认计数；使用
  禁止 Python 字节码缓存并显式启用 UTF-8 运行模式的当前 T-DOC 入口检查活动文档、链接、状态和方案关系；检查差异
  格式；生命周期关闭后对最终状态重新运行 T-DOC。

验证证明目标与副作用：

1. 静态所有权复核证明 F-01～F-08 的竞争正文已退出，必要合同与直接链接仍存在；只读取仓库文件。
2. 五样本映射复核只证明 suite 文档和夹具身份一致，不执行 `T-PROJECT-DOC-FORWARD`。
3. 当前 T-DOC 只证明文档机械规则；调用必须同时使用 `-B` 和 `-X utf8` 或由当前证据证明的等价方式，避免默认
   `__pycache__` 并固定文本运行编码。它不创建项目测试根、产品进程或外部输出。
4. 差异格式检查只读取当前工作树。
5. 关闭后的最终 T-DOC 绑定待办退出、方案归档、索引更新和空方案目录清理后的候选；中途结果不能替代。

`T-PROJECT-DOC-FORWARD` 为 `explicit`，本方案没有执行授权，也不把它列为关闭前必跑项；其状态保持 `not_run`。
全量测试、正式认证、产品进程、真实数据、网络、外部写入、Skill 安装和发布均不在验证范围。普通验证通过只能证明
本次活动文档职责和机械入口收敛，不能宣称模型运行时防回归、全项目测试完整或正式认证完成。

实施期分流：

- 原范围消费者漂移：若已列文档、锚点或机械断言直接消费本方案确定的不变量，且当前所有者唯一裁决结果，可在一跳
  范围内最小修正并重验。
- 临时环境阻断：Python 或必要只读文件不可用时保留首要错误和恢复条件，不改写为文档失败。
- 新范围问题：需要新语义、新权限、新副作用或确定路径外修改时保持方案活动并申请差额。
- 当前验证实现、确定路径或生命周期候选变化会使相应静态检查和 T-DOC 结果失效；没有最低未变证明时重新运行受影响项。
- 技术终态仅为本方案普通验证通过；正式认证、安装、提交、推送、PR、发布和外部回写仍未完成。

## 9. 授权与动作边界

用户本轮只授权保存本方案及必要待办链接。该授权不覆盖第 6 节文件实施、命令验证、待办状态转为实施中、归档、删除、
安装或 Git 写操作。

后续普通任务取得明确的按方案实施授权后，才可修改 13 份确定路径、执行第 8 节普通验证、据实记录结果，并在完成条件
满足且没有授权差额时按项目规则完成待办退出、方案归档、索引更新、最终门禁和本方案列明的空容器精确清理。

Commit（含 amend）、push、PR、Skill 安装、发布、全量测试、正式认证、认证／私有访问、上传、外部写入、付费或明显
大规模联网、产品进程、真实数据，以及方案外或高风险删除均不由后续普通实施授权推定。

## 10. 完成条件、最终复核与生命周期关闭

1. F-01～F-08 全部按确定归宿完成，发现、覆盖矩阵和最终处置仍为相同 8 项。
2. 需求保留完整用户可验收合同，但不再逐条复制 Skill 运行算法；两个 Skill 行为没有变化。
3. 设计只保存模块、路由、高层调用、验证拓扑和带状态 ADR 索引；被删除程序在唯一实现位置仍可达。
4. 术语、项目入口和测试协议只保留各自角色所需正文，通用规则通过直接链接到唯一所有者。
5. 前向 suite README 精确绑定五个标准 case，详细期望由现有夹具承接；没有把正文移动到另一份活动真源冒充收缩。
6. ADR 历史正文不被改写，部分替代状态及后继路径从 ADR 自身和设计索引都能识别。
7. 活动真源总内容足迹因删除列明重复、镜像和程序性说明而实际下降；默认入口加载量也因入口和术语收敛下降。结果
   分别描述，不使用行数或缩减百分比作为通过门槛。
8. 第 8 节普通验证据实通过；未执行的 explicit、全量和正式阶段保持 `not_run`。
9. 最终范围与漂移复核确认活动入口和所有者、13 份确定路径、已列直接消费者，以及修改路径新产生或改变的一跳直接
   链接／消费者；不递归、不扫描整个工作区、不重跑完整一致性审计或测试设计审计。
10. 完成条件满足且没有授权差额后，删除活动待办 `DOC-ACTIVE-TRUTH-CONTRACTION-20260813`，把本方案归档为
    `archive/docs/2026-08-13-DOC-ACTIVE-TRUTH-CONTRACTION-20260813-活动项目文档真源收缩方案.md`，更新
    `archive/docs/README.md`，确认并精确删除本次保存创建且已经为空的 `docs/方案/`，再运行最终状态 T-DOC。
11. 生命周期关闭不依赖 commit、push 或 PR；不生成实施后反哺报告。

### 收尾与联合复核

- 本方案主责与完成证明：活动项目文档唯一所有者与内容收缩；由 F-01～F-08 处置、普通验证和最终复核共同证明。
- 覆盖与残余：24/24 双遍已覆盖；动态验证保存时为 `not_run`；全量、正式认证和前向矩阵明确排除。
- 确定切片与就绪证明：13 份精确修改路径均有当前所有者、删除对象、一跳消费者和完成条件。
- 关闭时的状态消费者：`docs/已知问题与待做需求.md`、本活动方案、`archive/docs/README.md` 和最终 T-DOC。
- 关联方案、共享文件与对方职责状态：无其他活动方案；两个 Skill、夹具和 T-DOC 实现只作证据且不修改。
- 联合只读复核触发条件：实施修改新产生或改变一跳链接／消费者，或 ADR／suite 映射出现漂移时触发本方案内的有限复核。
- 最终范围与漂移复核：按完成条件第 9 项执行，不重跑本 Skill。
- 验收停点：普通验证失败、新语义、权限差额或确定路径外必改项出现时保持方案活动并停止关闭。
- 关闭边界与未运行验证：只关闭本待办和本方案；`T-PROJECT-DOC-FORWARD`、全量测试、正式认证、安装与 Git 交付保持未运行或范围外。

## 11. 本次候选评审与保存状态

- 用户选择：交付前两轮自查；实施完成后不生成反哺报告。
- 实际完成：`2/2` 轮。两轮均核对 24/24 双遍、F-01～F-08 集合、所有者、路径、一跳消费者、验证副作用、授权、
  完成条件和生命周期；未保留逐轮评语或修订轨迹。
- 用户最终决定：2026-08-13 明确确认只保存方案，不开始实施。
- 保存动作：仅创建本方案、创建此前不存在的活动方案容器并在现有待办文件登记链接；不修改任何方案目标文件。
- 保存后停点：机械核对方案内容、待办状态和实际链接与候选一致后立即停止。

## 12. 实施与验证结果

- 实施授权：用户于 2026-08-13 明确要求实施已保存收缩方案；待办在实施期间转换为 `实施中`，没有从方案存在
  反向推导授权。
- 确定修改：13 份路径全部按第 6 节实施。需求、设计、术语、项目入口、测试协议和前向 suite README 删除重复
  程序与实现镜像；七份早期 ADR 只新增状态／后继说明，原始决定正文、标题和路径保持不变。
- 内容足迹：六份收缩文档合计净减少约 684 行；七份 ADR 为解释替代关系合计增加 36 行。该数量只记录实际结果，
  完成证明仍以 F-01～F-08 的职责归宿和唯一所有者为准。
- 五样本静态映射：标准五项与 `contract-cases.md` 的五个精确标题一一对应；另外五个当前 CASE 已明确标为补充回归，
  不进入默认计数。
- 普通验证：实施中候选的 T-DOC 使用 `-B` 与 `-X utf8` 运行并通过，结果为 `0 warning(s)`；差异格式检查通过。
- 最终范围与漂移复核：实际目标修改仅为第 6.1 节 13 份路径；生命周期路径仅为待办、本方案和归档索引。两个 Skill、
  references、metadata、`contract-cases.md`、T-DOC 实现、其他源码／测试、其他仓库和 F2 均未修改；未发现新的
  一跳消费者、语义决定或授权差额。
- 未运行范围：`T-PROJECT-DOC-FORWARD`、全量测试、正式认证、Skill 安装、产品进程、真实数据、网络、外部写入和
  Git 交付均保持 `not_run` 或范围外；未生成反哺报告。
- 生命周期：活动待办已退出，本方案已归档并登记，保存阶段创建的空 `docs/方案/` 已在确认为空后精确删除；关闭后
  最终状态 T-DOC 使用 `-B` 与 `-X utf8` 运行并通过，结果为 `0 warning(s)`。
