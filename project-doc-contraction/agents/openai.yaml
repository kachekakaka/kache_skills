interface:
  display_name: "项目文档内容收缩审计与方案"
  short_description: "基于已验证一致性提交，只读查找重复、镜像、过程、错位和复杂内容"
  default_prompt: "使用 $project-doc-contraction 只做项目文档内容收缩；必须先让我明确给出已经完成一致性整改与验证的基线提交。若缺少基线或没有说清只要报告还是形成方案，先询问且不要开始项目调查；方案只在最终确认时保存并随后停止。"

policy:
  allow_implicit_invocation: false
