# 项目文档 Skill

本上下文定义项目文档结构与内容一致性处理中容易混淆的判断术语，使两个 Skill 使用相同语言但保持不同职责。

## Language

**职责不清**：
两个或更多当前文档争夺同一事实权威，或者某份文档没有明确的承接范围。
_Avoid_: 泛称“结构有问题”

**内容膨胀**：
文档纳入了超出自身职责的源码镜像、过程记录、历史快照或重复说明；不以篇幅长短判断。
_Avoid_: 文档太长

**位置不当**：
内容本身可以正确，但按现有职责应由另一份活动文档、历史记录、待办或临时方案承接。
_Avoid_: 内容错误

**临时活动方案**：
用户明确选择保存、尚待逐项裁决或选择施工方式的一份集中方案；它不是自动施工授权。
_Avoid_: 检查报告、自动执行计划

**主责方案**：
由一个审计目标的主责 Skill 保存、记录目标、证据、路径、动作边界和完成条件的活动方案，不预先编写具体脚本或
不必要的精确命令；副作用边界依赖调用方式时记录必要参数、受控输出或精确清理目标。Skeleton 的主责方案仍只承接
结构治理；consistency 的主责方案可以统一协调同一审计目标中已经通过切片就绪检查的文档、结构、产品、构建、测试、
CI 和安全实施切片，但不让其他 Skill 参与执行。
_Avoid_: 每种职责另起子方案、命令清单、自动实施计划

**覆盖／残余矩阵**：
本次检查实际填充的范围账本，逐项记录审计维度、本轮精确范围、已覆盖／无法验证／需要决策／未覆盖／不适用状态，
以及直接证据或最小残余；不是持久 profile，也不能用“已读完”替代。
_Avoid_: 空模板、只列发现项、用门禁通过代替未覆盖维度

**切片就绪检查**：
跨职责改动进入 consistency 综合方案确定范围前，对责任与契约、精确路径或受控候选及直接消费者、一层修改链、
验证义务、副作用与授权、完成条件和残余的有边界补证。未通过的候选留在同一方案待决。
_Avoid_: 仅凭建议把未知路径写成确定修改、为每个切片另起方案

**自包含方案**：
已经物化任务特定活动真源及所有者、冻结范围、已填写矩阵、决定、确定与可能路径、验证、可预见副作用处理、授权
边界及最终复核入口，使没有参加审计对话的普通任务只凭目标项目、方案和实施授权即可完成确定切片和关闭。
_Avoid_: 依赖原对话补范围、要求实施者再次调用审计

**最终范围与漂移复核**：
普通任务实施后确认最终活动入口和职责所有者，复核方案确定切片、已列直接消费者，以及修改路径新引入或改变的
一跳直接链接／消费者。它不递归展开、不扫描整个工作区，也不重跑完整内容或测试设计审计；新问题作为残余触发
再冻结或授权差额，而不是先关闭再另报。
_Avoid_: 只逐项勾选原发现、递归扩张、把实施复核变成第二次完整审计

**项目级 Skill 工具资产**：
仓库任意层级的 `.agents/skills/**`、`.cursor/skills/**`、`.claude/skills/**`、`.codex/skills/**`、
`.opencode/skills/**`、`.opencode/skill/**` 和 `.github/skills/**`，以及由用户、适用项目规则或冻结范围内精确
配置声明的其他 Agent Skill 根；默认不是活动项目文档，不进入文档审计，七类标准根也不进入通用 T-DOC。活动
声明可精确读取必要文件作为一轮实现证据，显式 Skill 审查则进入专项任务。
_Avoid_: 忽略整个工具目录、按文件名全局排除 `SKILL.md`、让通用门禁解析所有工具配置

**测试资产信息架构**：
测试治理入口、活动项所有者、集中测试治理根、模块内测试根及其直接依赖形成的当前结构关系。Skeleton 调查这些
关系，把未被入口或所有者承接的顶层测试目录列为待裁决候选，并把项目特定结论写入方案；但不预设目录树、共享层
或一项测试一目录，也不评价覆盖和断言质量。
_Avoid_: 固定测试家族、为了整齐拆 runner、递归审计全部测试实现

**结构机械断言**：
只验证路径、入口、生命周期或自动化副作用是否符合已确认结构决定的可执行断言；即使与产品断言位于同一文件，也只允许修改可明确分离的最小断言范围，不承接 API、数据、业务行为或测试覆盖充分性。
_Avoid_: 产品行为测试、泛称“受影响测试”

**结构机械断言漂移**：
结构机械断言仍约束已经移除、替代或收窄的路径、入口、生命周期或文档职责。由 skeleton 在退出依赖闭包中主动发现；consistency 仅在精确种子或直接证据已经暴露时记录并形成结构实施交接，不扩大默认审计范围或修改测试。
_Avoid_: 普通文档事实不一致、默认扫描全部测试

**验证执行账本**：
实际运行验证前在对话中建立的临时范围记录，核对方案已列的运行器、工作目录、现有依赖、输出位置和可预见副作用，
用于发现执行时漂移并判断是否需要申请差额；它不应替方案首次发现默认缓存、报告或快照，不写入仓库，也不跨任务继承。
_Avoid_: 持久测试 manifest、只列顶层命令

**退出依赖闭包**：
既有路径、入口、生命周期或文档职责被删除、替代或收窄前必须核对的直接消费者集合，包括引用、默认配置、启动接线、自动化触发、运维入口和结构机械断言；只有直接证据会改变退出结论时才继续扩展。
_Avoid_: 只修链接、无限全仓审计

**最终状态门禁**：
活动待办和方案退出、归档索引更新及空条件目录清理后，对最终仓库状态重新执行的结构验证；施工中途的通过结果不能替代它。
_Avoid_: 施工前验证、归档前通过

**分层验证结论**：
分别记录 T-DOC 机械门禁、内容一致性审计和项目测试的实际范围与结果；未运行项保持 `not_run`，任一层通过都不能替代另一层，其中项目测试还需区分受影响范围与完整 T-PROJECT。
_Avoid_: 用 T-DOC 通过宣称内容一致或项目测试完整通过

**副作用效果摘要**：
涉及自动化、外部副作用或授权边界的裁决前，用“会发生、不会发生、既有状态”说明实际后果的三行白话摘要。
_Avoid_: 只列 workflow 名称、权限字段或技术参数
