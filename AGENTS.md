# 项目协作约束

## 长期规则

- 与用户交流、项目文档、代码注释和提交说明使用简体中文；标识符、公开 API 和协议字段按语言生态使用英文。
- 本地只读操作默认允许，包括文件读取、搜索，以及不改变工作区、索引、引用或远端状态的 Git 查询；不因通过命令
  执行而另行授权。
- 未得到明确修改授权时保持只读。用户明确授权按已确认方案实施时，该授权同时覆盖方案确定范围内的文件新增、修改、
  重命名和删除、列明的普通验证及结果记录，以及完成条件满足后的待办状态更新、方案归档、索引更新和精确任务清理。
- 方案保存不构成实施授权。验证升级、新增副作用、候选发生实质漂移或需要新的语义决定时停止，只申请授权差额；
  方案关闭不以 commit、push 或 PR 为前提，也不推定这些操作。
- commit（含 amend）、push、PR、发布和 Skill 安装，以及会覆盖工作区、丢失修改、改写历史或改变远端状态的 Git
  操作必须明确授权；commit 授权包含确定变更的必要暂存，未请求 Git 交付时不主动改变索引、分支或历史。
- 未列明的联网、产品进程、真实数据、全量测试和正式认证仍不授权。方案未列明、超出确定范围、使用宽泛匹配或
  未解析路径、仓库外非任务产物、所有权不明或涉及真实数据的删除必须取得精确授权；确认方案列明的仓库内精确
  删除和本任务创建且所有权可验证的精确临时产物清理不重复申请。
- 普通验证是默认层级；全量测试和正式认证仅在用户明确选择后执行。
- 不得将 Cookie、Token、Key、密码、真实用户数据或原始运行现场写入仓库。

## 项目入口

- [项目术语](CONTEXT.md)
- [构建与交付](README.md#构建与交付)
- [项目文档](docs/README.md)
- [软件测试](SoftwareTesting/README.md)
