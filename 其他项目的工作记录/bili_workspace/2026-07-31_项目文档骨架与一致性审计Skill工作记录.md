# 项目文档骨架与一致性审计 Skill 工作记录

## 1. 记录目的

本记录复盘 `project-doc-skeleton` 与 `project-doc-consistency` 在 `bili_workspace` 上的一次连续使用，供后续优化两个 Skill 的职责、确认流程、验证范围和交接协议。

这是用户明确要求保存的历史执行记录，不是当前产品事实入口，也不是 `project-doc-consistency check` 默认生成的持久化审计报告。当前项目事实仍由[活动文档入口](../../docs/README.md)承接。

## 2. 基本信息

- 日期：2026-07-31
- 项目：`bili_workspace`
- 起始分支：`main`
- 起始提交：`181632f44a6fc52eb2e3838da04365c386bfe4e4`
- 起始状态：本地 `HEAD` 与 `origin/main` 一致
- 项目语言：简体中文文档与交流；提交标题使用英文
- Git 边界：本轮最终仅授权一次本地 commit；未授权 push、PR 或发布
- 产品与数据边界：没有启动产品服务、访问真实用户数据或执行发布

## 3. 用户请求链

1. 确认本地仓库是否最新；不一致时采用远端版本。
2. 显式调用 `project-doc-skeleton` 初始化当前项目骨架。
3. 多次确认骨架迁移和机械验证范围。
4. 询问并显式选择使用审计 Skill。
5. 使用 `project-doc-consistency check` 对当前项目做全量静态内容审计。
6. 先要求逐项确认修复方案，随后进一步明确：能够从代码和文档直接确定的事项不要询问，只询问真实取舍。
7. 明确 `verify.bat` 是 Windows 部署运行自检，不应承担文档检查。
8. 授权实施明确修复。
9. 要求创建本工作记录并做一次本地提交，供后续优化两个 Skill。

## 4. project-doc-skeleton 执行记录

### 4.1 场景判断

目标仓库已有完整产品代码、既有文档、测试、Windows/Docker 入口和历史归档，但未采用标准长期文档骨架，因此按“非标准既有项目首次迁移”处理。

项目未发现 `CONTEXT-MAP.md`、嵌套领域 `CONTEXT.md` 或多个独立业务上下文，适用单一上下文骨架。没有充分证据需要项目术语表，因此未创建 `CONTEXT.md` 或 ADR。

### 4.2 实施的结构变化

- 新建根级[协作约束](../../AGENTS.md)，只保存语言、授权、安全和长期入口。
- 建立 `docs/需求文档.md`、`docs/设计文档.md`、`docs/已知问题与待做需求.md`、`docs/软件测试.md` 和 `docs/字段契约.md`。
- 建立[测试治理入口](../../SoftwareTesting/README.md)、测试协议、安全约束、T-DOC 和 T-PROJECT suite。
- 将既有运维文档收口到 `docs/运维/`，增加二级索引。
- 将历史发布报告和已完成方案迁移到 `archive/docs/`，由单一归档索引登记。
- 保留既有事实正文，通过兼容入口路由；没有在骨架阶段合并、拆分或重写产品事实。
- 安装文档机械门禁及规则夹具，目标源码与 Skill 资产保持逐字节一致。

### 4.3 当时的验证

- 文档门禁规则夹具：23 项通过。
- 目标项目 T-DOC：通过，0 warning。
- Markdown 内部链接：通过。
- 骨架相关聚焦测试：19 项通过。
- 两个文档门禁源码与 Skill 资产逐字节一致。

### 4.4 后续暴露的骨架验证缺口

首次骨架验证没有覆盖 `tests/test_v060_plan_docs.py`。该测试仍引用迁移前的 `docs/plans/` 与 `docs/archive/`，直到后来运行完整 `verify.bat` 才暴露。

这说明“移动文档后搜索旧路径”和“聚焦测试通过”仍不足以证明所有项目测试已经适配。骨架 Skill 需要更明确地区分：

- 机械骨架已通过；
- 已运行的项目聚焦测试已通过；
- 未运行完整 T-PROJECT 时，其他测试中的旧路径仍属于未验证范围。

提交前重新比较通用资产时，还发现项目内 `test_doc_consistency_rules.py` 与当前 Skill 资产相差一行：当前资产已把类似 Token 的夹具字符串改为拼接形式，项目内仍是旧写法。骨架 Skill 按冲突规则停止并取得用户确认后，才将项目夹具升级为当前资产。这次拦截证明“目标资产不同时停止”有效，也说明资产一致性必须在最终提交前重新检查，不能只依赖初始化时的结果。

## 5. project-doc-consistency 执行记录

### 5.1 模式与范围

- 模式：`check`
- 范围：全量静态内容审计
- 文档：37 份活动 Markdown；10 份归档按历史时间语境处理
- 证据：代码、配置、Windows/Docker 脚本、GitHub Actions、测试契约和运行资产清单
- 未执行：联网、GitHub 状态查询、产品运行、真实数据操作和发布

### 5.2 明确不一致

1. `SECURITY.md` 仍声明维护版本为 V0.5.x，当前应用版本为 0.7.0。
2. 缺少 Node.js 时，`verify.bat` 和源码自检会跳过必需前端检查却仍报告完整成功，与测试协议冲突。
3. Windows 运行时构建工作流直接 commit 并 push `main`，与项目 PR/授权纪律冲突。
4. 旧 `release-v062.yml` 仍会在当前 `main` CI 完成后触发，却强制断言版本为 0.6.2。
5. V0.7 验收写“tag 触发 Docker 镜像”，实际流程是发布工作流显式 dispatch。
6. 当前发布前验证清单遗漏独立 T-DOC。
7. Windows 恢复文档先运行阻塞式 `start.bat`、后运行 `verify.bat`，顺序不合理。

### 5.3 需要决策

1. 新核心文档与既有“产品需求与架构基线”等文档仍存在兼容期职责重叠，无法只凭代码决定如何拆分正文。
2. T-PROJECT 规范命令当时内嵌 T-DOC，但 Registry 又要求全量测试分别运行两个 `full` 项，职责发生重复。

### 5.4 无法静态验证

- GitHub 上实际 CI、tag、Release 与 GHCR 镜像状态。
- 外部 Git Bundle、Windows 恢复包、校验文件和验证报告是否已经保存。
- 历史“门禁全绿”“原地升级通过”等动态结果是否绑定同一候选 SHA。

## 6. 用户确认与最终取舍

### 6.1 Node.js 与验证入口

用户指出 Node.js 是开发工具，不是应用运行依赖。最终确定：

- `verify.bat` 是 Windows 部署自检；不运行 T-DOC。
- 普通部署允许没有 Node.js，但必须明确显示前端开发检查未执行，不能声称完整发布验证通过。
- 发布严格模式通过 `BILI_VERIFY_REQUIRE_NODE=1` 要求 Node.js，缺失时阻断。
- Linux/macOS 的 T-PROJECT 是开发源码完整自检，Node.js 为前置条件。
- T-DOC 独立运行；T-PROJECT 不重复执行文档门禁。

### 6.2 Git 与自动化

用户明确表示是否推送通常由本人决定，因此未采用“机器人自动创建 PR”的建议。最终确定：

- Windows 运行时工作流只构建、验证并上传 Artifact。
- 工作流使用 `contents: read`，不 commit、不 push、不创建 PR。
- 是否把生成的运行包纳入仓库，由用户另行决定。
- 删除过期的 V0.6.2 发布工作流。

### 6.3 文档语义范围

- 本次只修复直接证据充分的冲突。
- 保留需求、设计、字段契约与既有事实文档的兼容路由状态。
- 不在本次任务中做大规模正文拆分、合并或 ADR 裁决。

### 6.4 确认方式调整

用户最初要求逐项确认，随后指出：代码和文档能够直接确定的事项不应反问，只询问无法唯一判断的事项。

这与两个 Skill 的理想行为一致，但实际交互前段仍产生了过多逐项确认。后续应把确认限定为：

- 会改变职责或实现方向的真实取舍；
- 不可恢复的结构动作；
- Git、发布、真实数据等新增授权；
- 证据无法唯一确定的语义决定。

## 7. 实际修复

- `verify.bat` 移除 T-DOC，增加可选 Node 与严格 Node 两种语义。
- `scripts/dev/verify-source.sh` 移除文档检查，并把 Node.js 设为 T-PROJECT 前置条件。
- CI 独立运行 T-DOC；Windows 发布验证启用严格 Node 模式。
- 运行时构建工作流改为只读并仅上传 Artifact。
- 删除 `release-v062.yml`。
- V0.7 发布工作流增加“现有 tag SHA 必须等于已验证 main SHA”的保护。
- 同步安全版本、Docker 触发说明、发布门禁、部署自检说明和恢复顺序。
- 更新骨架迁移后仍引用旧路径的测试。
- 完整测试暴露队列测试的固定 10 秒等待在当前 Windows 环境不足；改为等待最后一个任务完成，最多 30 秒。未修改队列业务代码。

## 8. 验证过程与结果

### 8.1 聚焦验证

- `tools/verify_source.py`：通过。
- T-DOC：通过，0 warning。
- Markdown 内部链接：通过。
- 发布、运行包、目录布局和门禁规则聚焦测试：57 项通过。

### 8.2 第一次严格 Windows 部署自检

第一次严格 `verify.bat` 得到：249 passed、11 skipped、5 failed。

失败分为：

- 1 项本轮产生的 `verify.bat` 混合换行；
- 3 项骨架迁移后仍引用旧文档路径；
- 1 项队列测试在 10 秒内只完成部分任务，单独复跑仍稳定超过 10 秒。

### 8.3 修复后验证

- Windows 批处理 CRLF 与归档路径测试：4 项通过。
- 队列测试单独复跑：通过，约 12 秒。
- 第二次严格 `verify.bat`：254 passed、11 skipped；所有 Node 单元测试通过；内置 Python、BBDown、FFmpeg 冒烟通过。
- 最终 T-DOC：通过，0 warning。
- 最终 Markdown 内部链接：通过。
- `git diff --check`：通过。
- 提交前资产复核首次发现规则夹具与当前 Skill 资产漂移；用户确认升级后，两个安装源码重新达到逐字节一致。

完整 pytest 的 2 条 warning 来自主动验证损坏配置和损坏索引恢复的测试，不是本轮失败。

## 9. 两个 Skill 的有效点

### 9.1 project-doc-skeleton

- 把结构、事实、语义决定分开，避免骨架初始化顺手重写产品事实。
- 固定入口、两跳可达性、归档唯一登记和 Registry 关系能够稳定约束文档增长。
- 明确修改、命令、commit、push、PR、发布分别授权，避免授权扩张。
- 通用门禁资产逐字节复制并配正反夹具，降低项目内分叉风险。
- 归档不再作为当前事实入口，历史快照得以保留。

### 9.2 project-doc-consistency

- “一致、不一致、无法验证、需要决策、未覆盖”五类结论能防止把证据不足误写成错误。
- 能区分规则真源、当前描述事实、历史记录和动态验证结果。
- 通过代码、配置、工作流和测试交叉核对，发现了单看 Markdown 无法发现的自动化问题。
- 对归档使用时间语境，没有因旧版本名称和行为自动判错。

## 10. 建议优化 project-doc-skeleton

1. **先给现有命令分类再接入测试治理。** 对每个既有入口明确标记“用户运行、部署自检、开发自检、CI、正式认证”。不得因名称包含 `verify` 就把 T-DOC 注入用户部署入口。
2. **建立依赖角色表。** 分别记录生产依赖、开发依赖、发布依赖和可选工具，并把 skip、blocked、failed、inconclusive 与具体入口绑定。
3. **迁移路径影响扫描应覆盖所有文本入口。** 文档移动后搜索代码、测试、工作流和脚本中的旧路径；仅运行少量聚焦测试时必须明确剩余风险。
4. **验证结论分层。** 分开报告“机械骨架通过”“聚焦项目测试通过”“完整 T-PROJECT 未运行”，避免用户把前两者理解为整仓通过。
5. **为已有部署入口增加保护夹具。** 当项目声明某入口面向最终用户时，骨架初始化不得自动加入文档、Node 或其他开发依赖。
6. **确认策略更严格地执行证据优先。** 能调查的事实集中调查；互不依赖的规则集中确认；只把真实取舍逐项交给用户。
7. **工作树换行策略纳入迁移检查。** Windows `.bat` 应保持 CRLF，活动 Markdown 保持 LF；补丁后在完整测试前先做字节级检查。

## 11. 建议优化 project-doc-consistency

1. **把“文档修订”和“实现修复”分开输出。** `sync` 只能改现有文档；发现脚本或工作流违反文档契约时，应生成明确的普通实现任务交接，而不是让用户误以为 sync 会改代码。
2. **增加命令角色一致性检查。** 对部署入口、开发入口、CI 和正式认证分别检查依赖、skip 语义和成功文案。
3. **增加发布自动化专项检查。** 检查活动旧版本 workflow、直接写 `main`、`[skip ci]`、tag 与候选 SHA 绑定、隐式 tag 触发和显式 dispatch 是否相符。
4. **先处理可证明项，再询问决策项。** 报告可按“直接修复、需要用户决定、外部动态无法验证”三组交付，避免逐条询问已经有唯一证据的事实。
5. **明确显式复盘文档例外。** 默认 check 不持久化报告是合理的；若用户明确要求为 Skill 优化保留执行复盘，可允许交给骨架 Skill 归档，并标明它不是当前项目真源。
6. **在全量审计中输出验证债务。** 对未运行的产品、网络、CI 和外部交付事实形成单独清单，不阻塞直接证据充分的修复。

## 12. 建议增加的 Skill 回归场景

- 现有 `verify.bat` 是部署入口，Node.js 不是运行依赖，T-DOC 必须保持独立。
- 同一个工具在本地部署可选、在发布认证必需，结果语义必须不同。
- 文档从 `docs/plans` 迁移到 `archive/docs/plans`，但未纳入聚焦集合的测试仍引用旧路径。
- 旧版本发布 workflow 在当前版本继续自动触发。
- 构建 workflow 拥有 `contents: write` 并直接 push `main`，但项目规则要求用户决定 Git 写入。
- 用户先要求逐项确认，随后要求只询问无法确定的事项；Skill 应及时收敛确认范围。
- 用户显式要求保存 Skill 执行复盘；审计 Skill 不直接持久化，由骨架 Skill 放入归档并登记。
- Windows 批处理补丁产生混合 CRLF/LF，机械检查应在完整测试前发现。

## 13. 未处理与后续边界

- 核心需求、设计和字段契约仍处于兼容路由期；是否拆分既有事实正文需要后续语义裁决。
- 未联网复核 GitHub 上实际 CI、tag、Release 或 GHCR 状态。
- 未核对用户外部保存的 Git Bundle、恢复包、校验文件和验证报告。
- 本轮没有 push、PR 或发布；远端状态不因本地提交自动改变。
- `update.bat` 的既有换行工作树差异不属于本轮内容修改，提交时应排除。
