# DOC-LIFECYCLE-20260804 文档生命周期实施工作记录

## 1. 记录目的

本记录复盘 `bili_workspace` 的文档骨架检查、实施方案保存、待办取舍、`DOC-LIFECYCLE-20260804` 实施与 T-DOC 验证结果，供后续维护 `project-doc-skeleton` 和继续处理项目文档治理时参考。

这是用户明确要求保存到 `kache_skills` 的外部工作记录，不是 `bili_workspace` 的当前事实入口，也不替代目标项目中的活动方案、待办清单或测试登记。

## 2. 基本信息

- 日期：2026-08-04
- 目标项目：`D:\Projects\python\bili_workspace`
- 使用的 Skill：`project-doc-skeleton`
- 工作记录仓库：`D:\Projects\python\kache_skills`
- 当前状态：`DOC-LIFECYCLE-20260804` 已完成获授权的文件实施与普通验证，仍保持“实施中”，尚未取得验收、收尾或归档授权
- Git 边界：未运行 Git 命令，未 commit、push 或创建 PR
- 产品与数据边界：未启动产品、未访问真实用户数据、未发布、未删除文件

## 3. 请求与执行顺序

1. 使用 `project-doc-skeleton` 检查目标项目的文档骨架并形成实施方案。
2. 按用户要求保存方案。
3. 复核 `TEST-ENTRY-20260803` 是否仍有必要，并比较两个活动待办的先后顺序。
4. 确定先实施 `DOC-LIFECYCLE-20260804`，后续再处理 T-PROJECT 隔离契约。
5. 在取得文件修改授权后，以普通项目实施方式完成文档生命周期收口；骨架 Skill 本身在方案交接后结束，不将实施动作冒充为 Skill 的自动 apply。
6. 在取得命令授权后，运行 T-DOC 规则夹具、目标门禁及两份标准资产逐字节比较。
7. 按用户要求将本工作记录保存到 `kache_skills`。

## 4. 骨架检查结论

目标项目已有根级协作约束、项目文档入口、测试治理入口、活动方案目录和历史归档，基础骨架完整。主要缺口不是继续扩充骨架，而是统一活动文档的生命周期语义和收尾证据：

- 文档类型与长期职责缺少集中说明。
- “待确认、实施中、待实施、暂缓”等状态与活动方案数量之间缺少稳定约束。
- 无问题检查记录的保存位置没有固定。
- 方案完成时缺少统一的联合复核、验证、遗留项和归档字段。

因此保存并实施 `DOC-LIFECYCLE-20260804`，没有借机重写产品事实或扩大测试治理范围。

## 5. TEST-ENTRY-20260803 取舍

`TEST-ENTRY-20260803` 仍有独立必要，不应直接取消。静态证据显示：

- `SoftwareTesting/SAFETY.md` 已要求使用外部测试根目录并禁止接触真实配置。
- T-PROJECT 现有入口尚未完整落实独立 run-id、外部输出、断言与清理契约。
- Windows 验证链会经过运行时准备和配置同步，存在写入仓库内运行配置的路径。
- Linux 源码验证从仓库根目录执行，未发现统一的隔离运行器。

本轮只澄清该待办没有活动方案、来源交接已登记为被替代计划；没有实施、关闭或删除它。顺序上先处理文档生命周期，是因为它能先稳定活动方案和收尾规则，随后 T-PROJECT 隔离方案可直接按新规则登记、实施和验收。

## 6. 实际文件变更

目标项目中修改了以下三份文档：

- `docs/README.md`
  - 增加文档生命周期分类。
  - 固定活动待办状态与活动方案数量约束。
  - 固定无问题检查记录位置为 `archive/docs/skills/`。
  - 增加六字段“收尾与联合复核”模板。
- `docs/已知问题与待做需求.md`
  - 保留 `TEST-ENTRY-20260803` 为“待确认”，明确其当前没有活动方案且隔离入口与契约证据仍待独立处理。
  - 将 `DOC-LIFECYCLE-20260804` 保持为“实施中”，并关联唯一活动方案。
- `docs/方案/DOC-LIFECYCLE-20260804-文档生命周期与收尾模板收口.md`
  - 保存初始证据、范围、选项、实施路径与验证字段。
  - 记录本轮实施结果和普通验证证据。

没有修改 T-PROJECT 实现、产品代码、测试资产或两份 T-DOC 标准资产。

## 7. 2026-08-04 普通验证结果

### 7.1 T-DOC 规则夹具

- 结果：通过。
- 数量：27 项测试全部通过。
- 进程退出码：0。

### 7.2 目标项目 T-DOC 门禁

- 结果：`文档机械一致性检查通过（0 warning(s)）。`
- 进程退出码：0。

### 7.3 标准资产逐字节比较

| 项目资产 | 结果 | 字节数 | SHA-256 |
| --- | --- | ---: | --- |
| `test_doc_consistency.py` | 与当前 Skill 资产逐字节一致 | 32651 | `6CF42DA26279A8BA01B7DAC196F852D855B2EAA81D829CD9048CC498F649170B` |
| `test_doc_consistency_rules.py` | 与当前 Skill 资产逐字节一致 | 21029 | `E11DA839658EF7DF2D242894BF107D4E80E60806B8C02EEB29A3F907E734B438` |

两份比较均以逐字节循环核对，并分别计算项目文件与当前已安装 Skill 资产的 SHA-256；文件长度与摘要均一致，比较命令退出码为 0。

## 8. 未运行与未声称的范围

- 未运行 T-PROJECT、全量测试、构建、产品命令或正式认证。
- 未验证外部 CI、发布平台或真实运行环境。
- 未执行 Git 状态调查、提交、推送或 PR 操作。
- 未把“普通 T-DOC 通过”表述为整仓、产品或发布验证通过。
- 未将 `DOC-LIFECYCLE-20260804` 标记为已完成，也未移动到归档。

## 9. 后续交接

1. 由用户按方案中的收尾模板进行联合复核，确认生命周期分类、状态约束和六字段模板是否可作为长期规则。
2. 只有取得明确验收与收尾授权后，才更新活动待办状态并归档 `DOC-LIFECYCLE-20260804` 方案。
3. 随后为 `TEST-ENTRY-20260803` 单独形成 T-PROJECT 隔离入口与契约证据方案；在方案确认前不实施或取消该待办。
