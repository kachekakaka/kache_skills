interface:
  display_name: "项目文档骨架检查与方案"
  short_description: "完整检查项目文档结构并保存可独立实施与关闭的方案"
  default_prompt: "使用 $project-doc-skeleton 完整检查当前项目的文档骨架；先报告，得到明确授权后只保存一份可交接方案并停止。"

policy:
  allow_implicit_invocation: false
