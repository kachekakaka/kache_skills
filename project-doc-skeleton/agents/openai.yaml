interface:
  display_name: "项目文档骨架管理"
  short_description: "集中确认独立事项后建立、迁移并机械检查长期文档骨架"
  default_prompt: "使用 $project-doc-skeleton 只读检查当前项目，集中确认互不依赖的事项，并在最终清单获批后建立或优化文档骨架。"

policy:
  allow_implicit_invocation: false
