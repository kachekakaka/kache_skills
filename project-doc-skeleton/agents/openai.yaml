interface:
  display_name: "项目文档骨架检查与方案"
  short_description: "只读完整检查项目文档结构，按请求报告或保存可实施方案"
  default_prompt: "使用 $project-doc-skeleton 检查当前项目文档骨架；如果我只调用 Skill 或没有说清只要报告还是形成方案，先询问交付路线且不要开始项目调查；路线明确后按对应只读流程执行，方案只在最终确认时保存并随后停止。"

policy:
  allow_implicit_invocation: false
